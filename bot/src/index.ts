// Bazarcha Telegram bot (grammY) — build step 4. Not wired yet.
// Plan: /start → free-text or voice → POST ${BACKEND_URL}/recommend → formatted top-3 reply
//       + inline web_app button opening MINIAPP_URL.
import "dotenv/config";

console.log("bot: not implemented yet — see README build order, step 4");
